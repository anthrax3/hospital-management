@model User

@{
    ViewData["Title"] = "Home";

}

<h2>@ViewData["Title"]</h2>

<h3>Welcome @Model.Fname</h3>

<div class="row">
    <div class="col-sm-3">
    </div>

    <div class="col-sm-5">

        <div class="form-group">

            <input type="button" class="btn btn-primary btn-lg text "  style="width:300px; " value="View Patient's History" onclick="location.href='@Url.Action("HistoryPage", "Doctors")'"  >
        
        </div>

        <div class="form-group">

            <input type="button" class="btn btn-primary btn-lg text  " style="width:300px; "  value="Edit Patient's Data" onclick="location.href='@Url.Action("EditPatientPage", "Doctors")'">
        </div>

      

        <div class="form-group">

            <input type="button" class="btn btn-primary btn-lg text " style="width:300px; " value="Contact Admin" onclick="location.href='@Url.Action("ContactAdminPage", "Doctors")'">
        </div>

        <div class="form-group">

            <input type="button" class="btn btn-primary btn-lg text  " style="width:300px; " value="Search Patient" onclick="location.href='@Url.Action("SearchPatientPage", "Doctors")'">
        </div>

        </div>

    <div class="col-sm-4">
    </div>


</div>

